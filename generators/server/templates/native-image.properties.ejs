Args = -H:Name=<%= dasherizedBaseName %> \
        -H:Class=<%=packageName%>.<%=mainClass%> \
        --no-fallback \
        -H:+TraceClassInitialization \
        -H:+ReportExceptionStackTraces \
        -H:IncludeResourceBundles=liquibase/i18n/liquibase-core \
        -H:ReflectionConfigurationResources=${.}/reflect.json \
        -H:ResourceConfigurationFiles=src/main/resources/${.}/resources.json

#       --report-unsupported-elements-at-runtime
